"""
Settings package — imports from the appropriate environment module.

The active settings module is determined by DJANGO_SETTINGS_MODULE:
- app.settings.development (default for local dev)
- app.settings.production (for deployed environments)
"""
